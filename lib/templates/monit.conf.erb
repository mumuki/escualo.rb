check host escualo-<%= name %> with address localhost
  start "/sbin/start <%= name %>"
  stop "/sbin/stop <%= name %>"
  if failed
    port <%= port %>
    protocol HTTP
    request "/"
    status > 199
    with timeout 30 seconds
  then restart